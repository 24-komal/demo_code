- hosts: all
  vars:
      - myname: "komal is very nice"
  tasks:
  - name: Check for the extra_var
    command: "echo {{ myname }}"
    register: myname
    tags:
      - name
    
  - debug:
       var: myname
     
