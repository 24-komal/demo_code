- hosts: all
  vars_files:
      - "group_vars/vars"
  vars:
      - myname: "komal is very nice"
  tasks:
  - name: Check for the extra_var
    command: "echo {{ myname }}"
    register: myname
    tags:
      - name
      
  - debug:
       var: myname
       
  - debug:
       var: mysurname
    
  - debug:
       msg: "This is the user: {{ tower_user_name }}"
     
