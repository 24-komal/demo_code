---

- hosts: localhost
  gather_facts: false

  vars:
    msg: 'hello'

  tasks:
    - debug:
        var: msg
  
    - command: 'ansible-runner -i localhost debug.yml'
      register: output

    - debug:
        var: output.stdout_lines
